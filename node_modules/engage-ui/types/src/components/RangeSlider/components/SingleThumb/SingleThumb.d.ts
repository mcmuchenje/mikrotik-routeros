import * as React from 'react';
export declare const CSS_VAR_PREFIX = "RangeSlider";
export declare function clamp(number: number, min: number, max: number): number;
export interface SingleThumbProps {
    label: string;
    labelAction?: any;
    labelHidden?: boolean;
    id: string;
    value: number;
    min: number;
    max: number;
    step?: number;
    output?: boolean;
    helpText?: React.ReactNode;
    error?: Error;
    disabled?: boolean;
    prefix?: React.ReactNode;
    suffix?: React.ReactNode;
    onChange(value: number, id: string): void;
    onFocus?(): void;
    onBlur?(): void;
}
export declare function SingleThumb(props: SingleThumbProps): JSX.Element;
